server:
  port: 8090

spring:
  application:
    name: shop-user-8090
  profiles:
    active: local
  cloud:
    nacos:
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yml
        group: SHOPOL_GROUP
